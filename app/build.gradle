plugins { id 'com.android.application' }

android {
    namespace 'ir.yaser.tasks'
    compileSdk 35

    signingConfigs {
        personal {
            storeFile file('yaser-personal.p12')
            storePassword 'YaserTasks1405'
            keyAlias 'yaser-tasks'
            keyPassword 'YaserTasks1405'
            storeType 'PKCS12'
        }
    }

    defaultConfig {
        applicationId 'ir.yaser.tasks'
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName '2.0.0'
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.personal
        }
    }
}
